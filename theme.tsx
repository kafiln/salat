import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Raleway",
    body: "Open Sans",
  },
  fontSizes: {
    xs: "0.75rem",
  },
});

export default theme;
